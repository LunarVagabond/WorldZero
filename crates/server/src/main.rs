//! Combined-process runnable binary — not yet implemented.
//!
//! Phase 1 target (docs/PROPOSAL.md, "Phased Roadmap"): wire `auth`, `character`,
//! `world`, `gateway`, and `content` together into a single process a self-hoster
//! can run end to end. `realm-directory`, `chat`, `transfer`, and `plugin-host`
//! come online in later phases.

fn main() {
    println!("worldzero server: not yet implemented (see docs/PROPOSAL.md, Phased Roadmap)");
}
