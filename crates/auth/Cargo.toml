[package]
name = "auth"
description = "Accounts, credentials, and session tokens behind a pluggable provider interface."
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
