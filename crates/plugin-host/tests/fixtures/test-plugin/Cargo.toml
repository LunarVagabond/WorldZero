[package]
name = "test-plugin"
version = "0.1.0"
edition = "2024"
publish = false

[workspace]

[lib]
crate-type = ["cdylib"]

[dependencies]
wit-bindgen = "0.60.0"

[features]
panic_on_load = []
escape_attempt = []

[profile.release]
opt-level = "s"
