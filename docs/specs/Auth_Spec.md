# Auth Spec

**Status:** placeholder — no content yet.

Corresponds to [Auth Provider Architecture](../PROPOSAL.md#auth-provider-architecture) in the proposal.

Will hold the `auth` crate's provider interface, the default self-contained provider, and how OAuth/SSO providers plug in.
