# Issue Workflow

**Status:** placeholder — no content yet.

Will document how issues get triaged, labeled, and claimed (see the `.github/workflows/claim-*.yml` issue-claiming automation).

One piece of this is already decided: architecture/design decisions made after initial commit get filed as their own issue with the `decision` label, not folded into whatever issue the discussion started in. Full convention — the required fields, and how a decision gets superseded later without rewriting history — lives in [`docs/architecture/Decisions.md`](../architecture/Decisions.md).
