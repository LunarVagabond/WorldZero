# Milestones & Releases

**Status:** placeholder — no content yet.

Will map the [Phased Roadmap](../PROPOSAL.md#phased-roadmap) onto concrete GitHub milestones once implementation starts.

**Note:** real build/test CI (`ci.yml`, `build.yml`, `cargo-audit.yml`, `codeql.yml`) is deliberately not set up yet — there's no Rust code or build system for it to run against. It gets added once Phase 0/Phase 1 implementation starts; only the community-process workflows (issue claiming, welcome bot, PR format checks) are live for now.
